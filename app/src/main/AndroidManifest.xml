<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.michaelgonzalez.goldrushtrail3">
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-feature android:glEsVersion="0x00020000"
                    android:required="true"/>
    <!-- Look below for the "mapApp" manifest file.  -->
    <!-- TODO:Get the first Extra Credit assignment to get the splash screen and remember to change the intent-filter to launch it first -->

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>

<!--
    <?xml version="1.0" encoding="utf-8"?>

    <manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.michaelgonzalez.mapapp">


     //The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
     //Google Maps Android API v2, but you must specify either coarse or fine
     //location permissions for the 'MyLocation' functionality.


    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">



         //The API key for Google Maps-based APIs is defined as a string resource.
         //(See the file "res/values/google_maps_api.xml").
         //Note that the API key is linked to the encryption key used to sign the APK.
         //You need a different API key for each encryption key, including the release key that is used to
         //sign the APK for publishing.
         //You can define the keys for the debug and release targets in src/debug/ and src/release/.

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

         //The first activity to display is called the launcher activity.  It is defined by the
         //intent-filter.  You need to know this if you want to display the splash screen AFTER
         //INITIALLY creating another launcher activity, which in this case is the map template
         //The name of the activity is just the extension of the package name.  EG if you wanted to ,
         //you could create the android:name attribute like so:
         <activity
            android:name="com.michaelgonzalez.mapapp.MapsActivity">


        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

    </manifest>
-->
